server:
  port: 8888
spring:
  datasource:
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/animal?useSSL=false

  jpa:
    show-sql: true
    database: mysql
    hibernate:
      ddl-auto: update
